{
  "name": "provisionpoint-teams-app",
  "version": "1.1.2",
  "private": true,
  "description": "Environment-specific Microsoft Teams app packages for ProvisionPoint.",
  "scripts": {
    "build": "node scripts/build.js all",   
    "build:au": "node scripts/build.js au",
    "build:ch": "node scripts/build.js ch",
    "build:eu": "node scripts/build.js eu",
    "build:uk": "node scripts/build.js uk",
    "build:us": "node scripts/build.js us",
    "build:uk-test": "node scripts/build.js uk-test",
    "build:uk-uat": "node scripts/build.js uk-uat",
    "validate": "node scripts/build.js all --validate-only",
    "npm-v-patch": "npm version patch && git push origin main:main && git push --tags",
    "npm-v-major": "npm version major && git push origin main:main && git push --tags"
  }
}
