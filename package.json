{
  "name": "provisionpoint-teams-app",
  "version": "1.1.5",
  "private": true,
  "type": "module",
  "description": "Environment-specific Microsoft Teams app packages for ProvisionPoint.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/build.js --validate-only",
    "npm-v-patch": "npm version patch && git push origin main:main && git push --tags",
    "npm-v-minor": "npm version minor && git push origin main:main && git push --tags",
    "npm-v-major": "npm version major && git push origin main:main && git push --tags"
  },
  "devDependencies": {
    "adm-zip": "0.6.0",
    "zod": "4.4.3"
  }
}
